/**
 * @param {string[]} strs
 * @return {string}
 */

var longestCommonPrefix = function (strs) {
  let longestPrefix = '';

  for (let i = 0; i < strs.length - 1; i++) {
    for (let j = i + 1; j <= strs.length - 1; j++) {
      let currentChar = strs[i];
    }
  }

  return longestPrefix;
};
